#!/usr/bin/env zsh

# ensure PATH entries remain unique after shell reload
typeset -gU PATH path FPATH fpath CDPATH cdpath MANPATH manpath

# ? give me some example code that might belong in ZSH ~/.zlogin file
